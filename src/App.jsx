import { useState } from 'react'
import './App.css'
// <img src={reactLogo}
/*
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
        </button>
        */
function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <p>testing!</p>
    </>
  )
}

export default App
